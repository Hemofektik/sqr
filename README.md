# Superquadriddle

TypeScript / Three.js port of the old XNA game. Currently only the main menu
and its superquadric background are implemented.

```bash
npm install
npm run dev
```

Then open http://localhost:8000
